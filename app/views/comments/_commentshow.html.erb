<% @comments.each do |comment| %>
  <p class="alert alert-info">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;
    </a>
    <%= comment.comment %>
    <% if params[:image_id].present? %>
      <%= link_to "",image_comment_path(comment.commentable_id, comment.id), class: 'glyphicon glyphicon-remove comment_delete', remote: true, :method => :delete %>
    <% else %>
      <%= link_to "",album_comment_path(comment.commentable_id, comment.id), class: 'glyphicon glyphicon-remove comment_delete', remote: true, :method => :delete %>
    <% end %>
  </p>
<% end %>