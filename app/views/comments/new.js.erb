if(<%= params[:album_id].present? %>)
{
	$('#comment_popup').html("<%= j (render 'commentcreate') %>");
	$('#myModal1').modal('show')
}
else
{
	$('.slide_commentbox<%= @commentable.id %>').html("<%= j (render 'for_image_comment') %>");
	$('.slide_commentbox<%= @commentable.id %>').toggle();
}